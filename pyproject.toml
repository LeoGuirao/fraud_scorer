[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fraud-scorer"
version = "0.0.1"
description = "Fraud Scorer"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
